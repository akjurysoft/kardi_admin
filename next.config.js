/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    //domains:['localhost', '3.91.105.55'],
    //domains: ["localhost", "0.0.0.0"],
    domains: ["localhost", "192.168.68.143"],
  },
};

module.exports = nextConfig;
